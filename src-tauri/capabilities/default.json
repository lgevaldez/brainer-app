{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for Brainer app",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}
